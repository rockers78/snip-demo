# Snip CLI

Zero-dependency Node CLI for the Snip backend.

## Usage

```bash
node cli.js help
node cli.js add https://example.com
node cli.js ls
node cli.js open abc123
```

Set `SNIP_API` to point at another backend origin if needed.